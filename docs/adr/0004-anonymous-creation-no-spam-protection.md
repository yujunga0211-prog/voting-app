# 익명 Poll 생성 + 공개 목록, 스팸 방지 없음

Poll은 로그인 없이 누구나 만들 수 있고, 만들어진 모든 Poll이 홈(`/`) 목록(최신순 20개)에 바로 노출된다. 과제·데모 규모에서는 단순함이 더 중요하다고 보고 rate limit 같은 스팸 방지는 넣지 않았다. 이는 알려진 위험이며, 악용되면 IP 기준 생성 횟수 제한을 먼저 추가한다. 만든 사람을 식별할 수 없으므로 Poll 수정·삭제도 제공하지 않는다.
